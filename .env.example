# ==================== DATABASE ====================
# PostgreSQL o MySQL connection string
# Esempio PostgreSQL: postgresql://username:password@localhost:5432/database_name
# Esempio MySQL: mysql://username:password@localhost:3306/database_name
DATABASE_URL="postgresql://user:password@localhost:5432/fdo_database"

# ==================== NEXTAUTH ====================
# URL del sito (in produzione sarà il tuo dominio)
NEXTAUTH_URL="http://localhost:3000"

# Secret per NextAuth (genera con: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl"

# ==================== DISCORD WEBHOOK (OPZIONALE) ====================
# URL del webhook Discord per notifiche
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN"

# ==================== DISCORD BOT API (OPZIONALE) ====================
# Token per autenticare richieste dal bot Discord
# Deve essere lo stesso nel bot Discord Python
DISCORD_BOT_API_TOKEN="your-secure-api-token-here"

# ==================== ADMIN API (OPZIONALE) ====================
# Secret per API amministrative
ADMIN_API_SECRET="your-admin-secret-here"

# ==================== NOTE ====================
# 1. Copia questo file in .env e compila con i tuoi valori reali
# 2. NON committare mai il file .env nel repository
# 3. In produzione, configura queste variabili nel pannello del tuo hosting
